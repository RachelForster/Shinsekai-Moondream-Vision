# Moondream 视觉插件依赖（与仓库根目录 requirements.txt 分离）
# 安装：pip install -r plugins/moondream_vision/requirements.txt
#
# PyTorch（含 CUDA 12.8 / 5080 等）：请先装 **再** 装本文件其余依赖，避免被解析成 2.11+。
# Moondream 本地 query 在 torch 2.11+cu128 上曾出现解码乱码；实践上 **2.7.1+cu128** 更稳。
#   pip install --upgrade pip
#   pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu128
# 验证：python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
#
# 若暂时只能用 torch 2.11+：插件会尝试在 CUDA 上强制 SDPA math 内核；仍异常可设 EASYAI_MOONDREAM_CUDA_SDPA_MATH_ONLY=1，
# 不需要规避时设 EASYAI_MOONDREAM_CUDA_SDPA_MATH_ONLY=0。
# transformers 5.x（如 5.7）与本仓库使用的 vikhyatk/moondream2 + trust_remote_code 组合会导致 query 解码乱码；请保持 4.x。
transformers>=4.51.1,<5
accelerate>=1.10.0
# 已由上一行单独安装 torch 时，勿用 pip -U 盲目升级 torch。
torch>=2.1.0
Pillow>=10.0.0
# INT8 / INT4（bitsandbytes）在 NVIDIA CUDA 环境下用于降低显存；未用量化可不装。
bitsandbytes>=0.43.1
mss
rapidocr-onnxruntime